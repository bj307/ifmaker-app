import 'package:flutter/material.dart';

class QrCodePonto extends StatelessWidget {
  const QrCodePonto({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
